<h1><%= @user.username %>'s events</h1>

<div><% @user.created_events.each do |e| %></div>
    <h2>Event</h2>
    <div><%= e.title %></div>
    <div><%= e.body %></div>
    <strong><%= e.date %></strong>
    <div><%= e.location %></div>
    <% end %>